import ComingSoon from '@/components/ComingSoon';

export const metadata = {
  title: 'Points Rules – Alpine Skiing',
  description: 'Explore alpine skiing v4.0 points rules'
};

export default function AlpineDocsPointsRulesPage() {
  return (
    <ComingSoon
      translationKey="alpine.docs.points-rules"
      backLink="/alpine"
    />
  );
}
